image:
  repository: acrfordevopspoc01adrian.azurecr.io/devops-project
  tag: "1.0.3"
  pullPolicy: IfNotPresent

replicaCount: 2

resources:
  requests:
    cpu: "150m"
    memory: "256Mi"
  limits:
    cpu: "300m"
    memory: "512Mi"

env:
  SPRING_PROFILES_ACTIVE: "prod"
  JAVA_OPTS: "-Xms256m -Xmx512m"

service:
  type: ClusterIP
  port: 80
  targetPort: 8080
